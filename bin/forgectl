#!/usr/bin/env bash
# forgectl wrapper — version resolution, constraint solving, and tool installation
# This script runs inside devbox environment (devbox.json adds bin/ to PATH)

set -euo pipefail

exec go run github.com/truvity/bar/forgectl/cmd/forgectl@28f14844d7e90680558722c8edaefecfdcebe945 "$@"
