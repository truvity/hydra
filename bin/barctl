#!/usr/bin/env bash
# barctl wrapper — executes barctl via go run from the private bar repo
# This script runs inside devbox environment (devbox.json adds bin/ to PATH)

set -euo pipefail

exec go run github.com/truvity/bar/bar/cmd/barctl@28f14844d7e90680558722c8edaefecfdcebe945 "$@"
